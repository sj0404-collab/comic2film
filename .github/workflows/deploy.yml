name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Verify syntax
        run: npm run check
      - name: Run smoke tests
        run: npm test
      - name: Build site
        run: npm run build:www
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        # только собранный www/, а не весь репозиторий: при path: .
        # в GitHub Pages попадали backend/main.py, relay/ и tests/
        with:
          path: www

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4